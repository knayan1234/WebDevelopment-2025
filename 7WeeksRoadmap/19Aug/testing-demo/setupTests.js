import "@testing-library/jest-dom";

// This file sets up extended matchers globally for all tests.
// It adds assertions like expect(element).toBeInTheDocument().
// No exports are needed—Vitest loads it automatically via vite.config.js.
